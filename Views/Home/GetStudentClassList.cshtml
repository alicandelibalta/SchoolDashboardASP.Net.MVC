


@model IEnumerable<SchoolDashboardMVC.View_Models.StudentClassVM>

@{
    ViewData["Title"] = "GetStudentClassList";
}

<h1>GetStudentClassList</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th> Student Name </th>
            <th> Student Class </th>

        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @item.Student.Name
               
            </td>
            <td>
                  @item.StudentClass.StudentClassName
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>

        </tr>
}
    </tbody>
</table>


